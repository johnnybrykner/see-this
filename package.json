{
  "name": "film-list",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend i && npm --prefix frontend run build"
  },
  "devDependencies": {
    "husky": "4.2.5",
    "lint-staged": "10.2.4",
    "prettier": "2.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,css,scss}": [
      "prettier --write"
    ]
  }
}
